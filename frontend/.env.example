# Environment Variables for Frontend

# API Base URL - Set this to your Netlify site URL in production
# For Netlify deployment, set to: /api (will use serverless functions)
# For local development, use: http://localhost:5000/api
REACT_APP_API_URL=/api

# Socket.IO Configuration (optional - for real-time notifications)
# Set to 'true' only for local development or if using external Socket.IO service
REACT_APP_SOCKET_ENABLED=false
REACT_APP_SOCKET_URL=http://localhost:5000
