[build]
  # Build command for the frontend
  command = "cd netlify/functions && npm install && cd ../../frontend && npm install && npm run build"
  # Directory to publish (React build output)
  publish = "frontend/build"
  # Directory for serverless functions
  functions = "netlify/functions"

[build.environment]
  # Node version
  NODE_VERSION = "20"

# API routes should go to serverless functions (must be first)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

# Redirect all other requests to index.html for React Router (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
